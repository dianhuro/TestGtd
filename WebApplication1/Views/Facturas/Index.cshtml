@model IEnumerable<WebApplication1.Models.Factura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Fecha
        </th>
        <th>
            Numero de Factura
        </th>
        <th>
            Nombre Cliente
        </th>
        <th>
           Total
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Fecha)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NumeroFactura)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NombreCliente)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Total)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.idFactura }) |
        @Html.ActionLink("Details", "Details", new { id = item.idFactura }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.idFactura })
    </td>
</tr>
}

</table>
